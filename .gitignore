# IDE and editor artifacts
.idea/
.vscode/
*.vscode%

# Data artifacts (keep shared caches ignored, but track specific packaged datasets)
data/*
!data/.gitkeep
# Keep committed datasets for reproducibility
!data/photonic_QCNN/
!data/photonic_QCNN/**
!data/QLSTM/
!data/QLSTM/**
!data/QEGM_rare_events/
!data/QEGM_rare_events/**
!data/Bandwidth_tunning/
!data/Bandwidth_tunning/**
!data/generative_quantum_embeddings/
!data/generative_quantum_embeddings/**
!data/bve_qnn/
data/bve_qnn/*
!data/bve_qnn/.gitkeep
!data/bve_qnn/sem_supervised_subset.npz
models/
!models/.gitkeep
!papers/**/models/
!papers/**/models/**
outdir/
!outdir/.gitkeep
.smoke_logs/
.smoke_envs/
# Temporary files
tmp_*/

# Common OS/editor noise
.DS_Store
*.swp
*.swo
*__pycache__*

# Python ignores
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# Local env & sandbox helpers
.venv
.home/

HQPINN/**/results/**/
HQPINN/doc/
HQPINN_*
# Private keys and other credentials. Never commit these: id_scaleway sat
# untracked at the repository root for days, one `git add -A` from being public.
id_*
!id_*.md
*.pem
*.key
!**/*.key.example
*_rsa
*_ed25519
.ssh/

# Exploratory work kept beside the repository but deliberately not part of it.
notes/

# Files delivered into this folder by the Claude desktop app. Not repository
# content: they land here because the repository is the connected folder.
Claude outputs/

# Transfer artifacts used to move files onto this machine
_*.tgz
_template_leftovers/
_package_note.txt
