{
  "description": "Loop topology at m=20: the fibre loop modelled as a rail with memory, which is what ORCA's released simulator implements (validated to 1e-12 in tests/test_tbi.py). The paper's own parameter count implies the pairwise chain instead. Same problem, same hyperparameters, so this isolates the circuit model.",
  "seed": 42,
  "dtype": "float64",
  "outdir": "outdir",
  "device": "cpu",
  "problem": {
    "family": "knapsack",
    "m": 20,
    "instances": 200,
    "instance_seed_base": 0
  },
  "circuit": {
    "delays": [
      1,
      3,
      9
    ]
  },
  "training": {
    "updates": 200,
    "samples": 50,
    "lr_theta": 0.01,
    "lr_alpha": 0.05,
    "shift": 1.5707963267948966,
    "shift_scale": 1.0
  },
  "method": {
    "name": "bbs",
    "source": "boson",
    "rate_scale": 1.0,
    "topology": "loop"
  },
  "logging": {
    "level": "info",
    "save_config_snapshot": true
  }
}